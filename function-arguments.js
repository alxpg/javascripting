function math (a, b, c) {
  console.log(a + b * c);
}
math(53, 61, 67);

// Imprime: 4140 porque la función math toma tres argumentos y realiza la operación a + b * c. Se ejecuta el código en el archivo function-arguments.js.