const roundUp = 1.5;
const rounded = Math.round(roundUp);
console.log(rounded);

// imprime 2 porque Math.round redondea el número al entero más cercano. En este caso, 1.5 es el número más cercano a 2.